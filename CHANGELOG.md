# Changelog

## 0.3.1-rc.3 - 2026-07-30

- fix: document Windows prerelease removal instead of encoding versions (`27a5c00`)

## 0.3.1-rc.2 - 2026-07-30

- fix: set user.dir so packaged apps find data/ outside macOS bundles (`4f0d3d9`)
- fix: fail packaging when data/ is not staged beside the jars (`0d12ce2`)
- ci: smoke test the exported app before packaging (`0fe4549`)

## 0.3.1-rc.1 - 2026-07-28

- fix: stage release installers outside the checkout to keep the tree clean (`d087c34`)
- ci: drop macos-x86_64; Intel runners never schedule and would stall releases (`c7a0a8e`)
- ci: add dry_run option that builds and packages without releasing (`32c87dc`)
- ci: consolidate release into plan/build/package/release pipeline (`b53cded`)
- ci: export per host in build-test; Processing cannot cross-export macOS (`f1c4c55`)
- fix: search to depth 4 for Processing mode jars (`50f800c`)
- fix: assemble Processing classpath by search so Linux layouts work (`88ace3d`)
- ci: add dispatch-only build-test workflow for iterating on packaging (`d100e30`)
- ci: build self-contained installers per platform with jpackage (`2d9d2fc`)
- ci: add dispatchable release workflow building all variants and Linux packages (`d9de55a`)
- ci: build and attach Linux .deb/.rpm packages to releases (`7e73657`)
- docs: state the verified Java version range for release binaries (`c2ca14c`)
- docs: note the Java runtime requirement for release binaries (`51b903f`)
- chore: export release builds without an embedded JRE (EMBED_JAVA=0) (`21d44a3`)
- chore: add dist task packaging platform builds as release artifacts (`2670bc4`)

## 0.3.0 - 2026-07-27

- chore: add mise release task catalog (`7300fca`)
- fix: crashes, data loss and dead code found in a full-codebase audit (`03095d9`)
- feat: load Petmate .petmate workspaces and fix Merge replacing the image (`65d7be5`)
- feat: add help overlay listing all shortcuts from a central registry (`abf7583`)
- feat: reduce oversized charsets by agglomerative clustering (merge+interpolate nearest glyphs) (`f55d84c`)
- chore: update gitignore (`0f23ca2`)
- refactor: remove dead file-dialog code from java.pde, rename infidel->wasInField and Machine.machine->id, drop done TODOs (`17c7d02`)
- refactor: move image tracing (canvas mutation) out of Machine into an editor layer (`b159df8`)
- refactor: group drawing-tool globals (pen/current/curidx) into a Tool object (`7a8ba67`)
- refactor: group selection/clipboard and cursor/typing globals into Sel and Cur objects (`b93f112`)
- refactor: group window/layout globals into a View object (`dd7431e`)
- refactor: extract a shared commands layer; buttons and keys dispatch to the same cmd_* functions (`8232428`)
- refactor: replace event-flag soup with a single deferred-command queue (`55c9053`)
- feat: best-effort charset when an image has >256 chars, with a popup explaining the estimate (`94c3b54`)
- feat: export charset as C64 .prg with load-address prompt, and show key shortcuts in button help (`7beba89`)
- feat: add hover help text to all remaining toolbar buttons (`7668581`)
- fix: trace image / load charset on the animation thread to avoid races with draw() (`68d0716`)
- feat: match best palette colours per character when tracing an image (`7325b2d`)
- feat: add Image button to trace any image into a charset; move Machine/Zoom to their own row (`7678c6b`)
- feat: reconstruct the source image on the canvas when generating a charset from a hires image (`e705c2a`)
- fix: reject short/non-image .c files gracefully instead of crashing in load_c (`a3357e3`)
- fix: load charsets with fewer than 256 tiles (pad blank), error visibly above 256 (`b64119d`)
- feat: save charset as .png with chosen chars-per-row; clearer charset failure messages (`d179ede`)
- feat: build a C64 charset from an arbitrary hires image, dedup identical b/w blocks (`2bf4e56`)
- feat: runtime machine switching and zoom via Machine/Zoom buttons and drag-to-resize (`872983a`)
- fix: restore selector() dialog for platform picker and confirmations (`5a61847`)
- feat: load charset from any png that tiles into 256 8x8 chars (`fd668d3`)
- fix: wire Load .prg import dialog (`1b0a2d3`)
- fix: register UI button clicks on mouseReleased so first click always works (`aadb034`)
- feat: add zoom and bg/border cycling keyboard shortcuts (`c18fb4f`)
- refactor: use selectInput/selectOutput for file dialogs (`c583ea9`)
- refactor: rewrite preview as standalone PreviewWindow (`b8930b1`)
- fix: fall back to C64 when machine pref is unknown (`32ad968`)
- style: highlight toolbar buttons on hover (`d081be8`)
- feat: use andalemono14 as the UI font (`dddf871`)
- build: add console build.sh with macos arm64 jre fix, ignore build/ and .idea/ (`e4b1219`)
- Add missing parenthesis (`27e24da`)
- Cherrypick r954, r955 from svn (`0fb0e43`)
- Refactor charset initialization (`4d825a8`)
- Add script for converting charsets (`f252899`)
- Add support for button tooltips (`b0bd34a`)
- Add placeholder button for Refresh charset (`87abab0`)
- Enable charset load (`5fa2be9`)
- Trigger fileselector event for loading charset (`e4feadf`)
- Update UI: Buttons in 4 rows. Add button for loading charset. (`cb0982f`)
- Updates to README. Added a contributing guide. (`e981fb0`)
- Removed original release script. Added new release script to extras. (`acce81d`)
